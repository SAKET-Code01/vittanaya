import React from 'react';
import { useWorkspace } from '../context/WorkspaceContext';
export default function FinancialPlanPage() { const { financialData } = useWorkspace(); return <section className="rounded-2xl border border-slate-200 bg-white p-6 sm:p-8"><p className="text-xs font-bold uppercase tracking-wider text-emerald-700">Financial Plan</p><h1 className="mt-2 text-2xl font-black text-slate-900">Financial structuring</h1><p className="mt-3 text-sm text-slate-600">This workspace uses the financial position already captured for the active business.</p><div className="mt-5 grid gap-3 sm:grid-cols-2 text-sm"><div className="rounded-lg bg-slate-50 p-4">Minimum cash buffer: ₹{Number(financialData?.min_cash_buffer || 0).toLocaleString('en-IN')}</div><div className="rounded-lg bg-slate-50 p-4">Expected outflow: ₹{Number(financialData?.expected_outflow || 0).toLocaleString('en-IN')}</div></div></section>; }
